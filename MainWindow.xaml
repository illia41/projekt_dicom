<Window x:Class="DicomViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DICOM Viewer" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Wczytaj plik DICOM" Click="LoadDicom_Click" Height="30" Margin="0,0,0,10"/>

        <Slider x:Name="SliceSlider" Grid.Row="1" Minimum="0" Maximum="0" ValueChanged="SliceSlider_ValueChanged" Height="30" Margin="0,0,0,10"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Image x:Name="ImageAxial" Grid.Column="0" Margin="5"/>
            <Image x:Name="ImageSagittal" Grid.Column="1" Margin="5"/>
            <Image x:Name="ImageCoronal" Grid.Column="2" Margin="5"/>
        </Grid>
    </Grid>
</Window>
